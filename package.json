{
  "name": "buildnest",
  "version": "0.1.0",
  "private": true,
  "description": "BuildNest monorepo — Plan. Build. Track.",
  "scripts": {
    "prepare": "husky",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,css,md,yml,yaml}": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0",
    "prettier": "^3.9.5"
  }
}
